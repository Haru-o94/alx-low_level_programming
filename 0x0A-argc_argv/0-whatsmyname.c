#include <stdio.h>
/**
 * main - prints its name,with new line.
 * @argc: integer number
 * @argv: array value
 * Return: 0 - success.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
